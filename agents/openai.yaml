interface:
  display_name: "AI MATLAB Skill"
  short_description: "Control MATLAB from auditable agent workflows"
  default_prompt: "Use $ai-matlab-skill to choose the narrowest MATLAB control route, verify availability, run or dry-run safely, and report reproducible evidence."
policy:
  allow_implicit_invocation: true
